// Fill these in to enable optional features. Leave blank to keep them off.
//  - Supabase keys  → cloud sync + Edge Functions (see supabase/SETUP.md)
//  - kiteApiKey     → "Connect Zerodha" button (see supabase/KITE.md);
//                     the matching api_secret lives ONLY as a Supabase secret.
export const environment = {
  supabaseUrl: '',
  supabaseAnonKey: '',
  kiteApiKey: '',
};
